# 기존 데이터 보존 확인

이 버전은 기존 Firebase 프로젝트 `status-board-d2b05`와 동일한 Firestore 컬렉션을 사용합니다.

- `categories`
- `tasks`
- `members`

화면 로딩, 로그인, GitHub Pages 배포 과정에는 데이터를 지우는 동작이 없습니다.
새 `assignee` 필드는 선택 사항이며 기존 문서에 자동으로 쓰지 않습니다. 값이 없는 기존 항목은 화면에만 `미지정`으로 표시됩니다.

데이터 삭제는 편집 권한이 있는 사용자가 화면에서 삭제 버튼을 누르고 확인한 경우에만 실행됩니다.
`기본 목록 만들기` 역시 카테고리와 항목이 모두 비어 있을 때 관리자가 직접 선택해야 실행됩니다.
